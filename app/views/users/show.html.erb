<!-- app/views/users/show.html.erb -->
<p>User: <%= @user.username %></p>
<h3>Events Created by User:</h3>
<ul>
  <% @events.each do |event| %>
    <li><%= link_to event.name, event_path(event) %></li>
  <% end %>
</ul>
